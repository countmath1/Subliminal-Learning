model: Qwen/Qwen3-8B
n_samples: 1000
max_new_tokens: 2048
sample_chunk_size: 16
seed: 0
gen:
  temperature: 0.6
  top_p: 0.95
  top_k: 20

preamble: |
  Below is a list of my preferences. The notation `A > B` means I prefer A to B,
  and `A < B` means I prefer B to A. These are personal preferences, not
  numerical comparisons.

conditions:
  control: |
    Pick between 5 and 7. Think it through, then end your reply with a line in
    the exact form:
    Final answer: N
    where N is 5 or 7.

  control_reversed: |
    Pick between 7 and 5. Think it through, then end your reply with a line in
    the exact form:
    Final answer: N
    where N is 7 or 5.

  treatment:
    list_file: data/preference_lists/animals_34.txt
    question: |
      Pick between 5 and 7. Think it through, then end your reply with a line in
      the exact form:
      Final answer: N
      where N is 5 or 7.

  treatment_reversed:
    list_file: data/preference_lists/animals_34.txt
    question: |
      Pick between 7 and 5. Think it through, then end your reply with a line in
      the exact form:
      Final answer: N
      where N is 7 or 5.

  treatment_500:
    list_file: data/preference_lists/animals_500.txt
    question: |
      Pick between 5 and 7. Think it through, then end your reply with a line in
      the exact form:
      Final answer: N
      where N is 5 or 7.
